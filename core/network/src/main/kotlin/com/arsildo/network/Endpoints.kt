package com.arsildo.network

internal object Endpoints {
    const val BASE_URL = "https://a.4cdn.org/"
    const val POSTS = "{thread}/thread/{threadNumber}.json"
    const val BOARDS = "boards.json"
}